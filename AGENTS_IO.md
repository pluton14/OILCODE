# Вход и выход агентов — рабочий справочник

Только входы и выходы. Объяснения процесса — в `AVT_TAGS.md`, `HT_TAGS.md`,
`BLENDING.md`. Методы — в `PLAN.md`.

---

## 0. ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРВЫМ: имена тегов пересекаются

**Восемь коротких имён существуют на обеих установках и означают разное.**

| Имя | На АВТ | На 24-2000 |
|---|---|---|
| `T6` | Температура низа К-1, ≈229 | температура, ≈363 |
| `F9` | Расход обессоленной нефти, 2-й ход, ≈212 | газ поддува в К-201, ≈219 |
| `T11` | 3-е ЦО после Т-46, ≈65 | температура, ≈364 |
| `F14` | Расход 1-го ЦО в К-2, ≈253 | ≈6 |
| `T18` | 1-е ЦО из К-2, ≈157 | ≈68 |
| `F19` | Острое орошение К-2, ≈66 | ≈215 |
| `F25` | Бензин после Т-26, ≈19 | ≈13 189 |
| **`F26`** | **Пар в К-6 — МЁРТВ** | **Расход сырья на ГО, ≈258 — ключевой** |

**Соглашение: везде пишем с префиксом установки** — `AVT:T11` и `HT:T11`.
В коде теги 24-2000 живут как `HT_*`, обращение только через
`config.tag_key(tag, unit)`.

**Служебные значения-заглушки: `307.00` и `251.00`.** Это не измерения.
Выбрасывать до любого расчёта.

---

## 1. Кто кому что передаёт

| От кого | Кому | Что передаётся |
|---|---|---|
| файлы | Агент данных | сырьё |
| Агент данных | всем | `ProcessState` + вердикт пригодности |
| Агент качества | Оптимизация, Блендинг, Оркестратор | прогноз качества, риск |
| Агент надёжности | Оптимизация, Оркестратор | ограничения, индекс износа |
| Агент оптимизации | Блендинг (по каждому сценарию), Оркестратор | сценарии |
| Агент блендинга | Оптимизация (обратно), Оркестратор | допустимость и стоимость |
| Оркестратор | оператор, дашборд | 7 блоков |

Право вето: **Агент данных**, **Агент надёжности**, **Агент блендинга**.

---

## 2. АГЕНТ ДАННЫХ

### IN — сырые источники

| Файл | Что берём |
|---|---|
| `docs/data/data/avt_tags.csv` | 71 тег, шаг 10 мин |
| `docs/data/data/242000_tags.csv` | 26 тегов, шаг 10 мин |
| `docs/ЛИМСы…xlsx` | 6 точек отбора, 54 колонки-пары |
| `docs/Выгрузка ПАК…xlsx` | 2 сигнала |
| `docs/Теги_хакатон.xlsx` | справочники КИП, ЛА, ПАК, ВАК |

### IN — параметры (не теги)

| Параметр | Значение | Источник |
|---|---|---|
| Задержка публикации ЛИМС | 4 ч | организаторы |
| Заглушки | 307.00, 251.00 | найдено в данных |
| Признак остановки АВТ | `AVT:F7+F8+F9` < 5 % медианы | найдено |
| Признак остановки ГО | `HT:F26` < 5 % медианы | найдено |
| Порог залипания | без изменений > 6 ч | допущение |

### OUT — `ProcessState`

| Поле | Тип | Смысл |
|---|---|---|
| `timestamp` | datetime | момент решения |
| `kip` | dict[str, float] | значения тегов, ключи с префиксом `HT_` |
| `quality` | dict[str, Measurement] | показатели качества |
| `quality[x].value` | float | значение |
| `quality[x].source` | LIMS / PAK / VAK | откуда |
| `quality[x].measured_at` | datetime | момент **отбора** |
| `quality[x].age_hours` | float | возраст от отбора |
| `data_quality.unit_running` | bool | установка работает |
| `data_quality.missing_tags` | list | нет значения |
| `data_quality.stale` | list | устаревшие анализы |
| `data_quality.sentinels` | list | попались заглушки |
| `data_quality.overall` | ok / degraded / insufficient | **вердикт** |
| `data_quality.notes` | list[str] | причины для объяснения |

**Вето:** `overall == insufficient` → система отказывается без опроса остальных.

---

## 3. АГЕНТ КАЧЕСТВА

### IN — теги режима гидроочистки

| Тег | Медиана | p5..p95 | Что это |
|---|---|---|---|
| `HT:T5` | 370.3 | 353–385 | температура слоя (Р-201 выход) |
| `HT:T6` | 363.2 | 346–380 | температура слоя |
| `HT:T11` | 364.0 | 349–379 | температура слоя |
| `HT:F15` | 3401 | 2996–3612 | квенч |
| `HT:P13` | 3.92 | 3.7–4.0 | давление, МПа |
| `HT:F26` | 258.1 | 184–297 | расход сырья |
| `HT:F2` | 93 315 | 83 011–103 054 | циркуляционный газ |
| `HT:P24` | 0.59 | 0.6 | свежий водород |

### IN — теги АВТ: что придёт на вход гидроочистки

| Тег | Медиана | p5..p95 | Что это |
|---|---|---|---|
| `AVT:T66` | 254.2 | 243–265 | переток в К-9, вход дизотбора |
| `AVT:F28` | 275.1 | 150–375 | пар в К-9 (отпарка) |
| `AVT:F30` | 128.4 | 104–153 | фр. 290–350 |
| `AVT:F32` | 81.7 | 58–96 | фр. 240–290 |
| `AVT:W70` | 100.3 | 81–119 | фр. 290–350, массовый |
| `AVT:T71` | 304.4 | 299–312 | температура фр. 290–350 |
| `AVT:T33` | 338.3 | 335–340 | низ К-2 |
| `AVT:T20` | 113.8 | 113–116 | верх К-2 |

### IN — лабораторные и поточные показатели

| Источник | Показатель | Замеров | Роль |
|---|---|---|---|
| ПАК `24-2000:Mg.Sulfur` | сера, мг/кг | 189 649 | **целевой**, шаг 10 мин |
| ПАК `24-2000:D15` | плотность | 75 455 | только с 05.03.2025 |
| ЛИМС ГО точка 2 | `Mg.Sulfur` | 1 462 | контрольный факт |
| ЛИМС ГО точка 2 | `95%.T` | 1 292 | второй показатель спецификации |
| ЛИМС ГО точка 2 | `CetaneNumber` | **42** | **модель невозможна** |
| ЛИМС ГО точка 1 | `Mass.Sulfur` | 132 | сера в сырье, % масс |
| ЛИМС ГО точка 1 | `95%.T` | 1 276 | тяжесть сырья |

### IN — производные признаки (считаем сами)

| Признак | Формула |
|---|---|
| `WABT` | среднее по `HT:T5`, `HT:T6`, `HT:T11` |
| `AVT_diesel` | `AVT:F30 + AVT:F32` |
| `S_lag_10м/30м/1ч/3ч` | сдвиги ПАК-серы |
| `S_mean_3h` | скользящее среднее серы за 3 ч |
| `T5_3h`, `T5_d24h` | среднее за 3 ч; отклонение от среднего за сутки |
| `cat_age_h` | часов с последней остановки |
| `lims_age_h` | возраст анализа с учётом задержки 4 ч |

### OUT — `QualityAssessment`

| Поле | Тип | Смысл |
|---|---|---|
| `sulfur_now` | float | сера сейчас, мг/кг |
| `sulfur_now_source` | PAK / VAK | измерено или восстановлено |
| `sulfur_forecast` | dict[horizon, float] | горизонты 1 ч и 3 ч |
| `sulfur_forecast_ci` | dict[horizon, (low, high)] | интервал |
| `t95_now` | float | Т95, °C |
| `confidence` | float 0..1 | доверие |
| `blendable` | bool | сера ниже порога, при котором блендинг ещё вытянет |
| `notes` | list[str] | оговорки |

**Метод (см. `PLAN.md`):** прогноз = инерция + поправка на действие. Эффект
изменения режима — физическая зависимость, не подгонка по истории.

---

## 4. АГЕНТ НАДЁЖНОСТИ

### IN — теги износа

| Тег | Медиана | p5..p95 | Что это |
|---|---|---|---|
| `HT:W10` | 2.80 | 0.8–4.5 | **перепад давления Р-202** — закоксовывание |
| `HT:T5` | 370.3 | 353–385 | уровень температуры |
| `HT:T6` | 363.2 | 346–380 | температура слоя |
| `HT:T11` | 364.0 | 349–379 | температура слоя |
| `HT:F2` | 93 315 | 83–103 тыс. | водород подавляет коксообразование |
| `HT:P24` | 0.59 | 0.6 | свежий водород |
| `HT:F26` | 258.1 | 184–297 | нагрузка |

### IN — производные

| Признак | Формула |
|---|---|
| `WABT` | среднее по трём температурам слоёв |
| `cat_age_h` | часов с последней замены катализатора |
| `WABT_rate` | рост WABT, °C в месяц, внутри текущего цикла |
| `dP_rate` | рост `HT:W10` внутри цикла |

### IN — известные границы циклов

| Остановка | Тип |
|---|---|
| 17.03 – 17.04.2024 | весь комплекс, замена катализатора |
| 16 – 24.04.2026 | только гидроочистка |
| 19 – 29.06.2026 | весь комплекс |

### IN — параметры

| Параметр | Значение | Статус |
|---|---|---|
| Предел WABT (End of Run) | ~400 °C | отраслевая практика, **допущение** |

### OUT — `ReliabilityAssessment`

| Поле | Тип | Смысл |
|---|---|---|
| `wabt` | float | текущее значение, °C |
| `wabt_rate_per_month` | float | скорость деградации |
| `headroom_c` | float | запас до предела, °C |
| `remaining_life_months` | float | оценка остатка цикла |
| `severity_index` | float 0..1 | тяжесть режима |
| `severity_class` | normal / elevated / severe | класс |
| `risk_factors` | list[{tag, issue, severity}] | что именно плохо |
| `regime_allowed` | bool | **вето на ужесточение** |
| `constraints` | list[{tag, min, max, reason, is_assumption}] | пределы для оптимизации |

**Вето:** `regime_allowed == false` → нельзя ужесточать режим, какой бы ни была
выгода.

---

## 5. АГЕНТ ОПТИМИЗАЦИИ

### IN — рычаги (текущие значения)

| Тег | Медиана | Шаг перебора | Направление эффекта |
|---|---|---|---|
| `HT:T5` | 370.3 | ±1 %, ±2 % | выше → серы меньше, износ быстрее |
| `HT:F26` | 258.1 | ±1 %, ±2 % | выше → меньше время контакта → серы больше |
| `HT:F15` | 3401 | ±1 %, ±2 % | выше квенч → слой холоднее → серы больше |
| `HT:P13` | 3.92 | ±1 %, ±2 % | выше → серы меньше |
| `HT:F2` | 93 315 | ±1 %, ±2 % | выше → лучше очистка, меньше кокса |
| `AVT:T33` | 338.3 | ±1 % | сдвигает границу отбора |
| `AVT:T20` | 113.8 | ±1 % | сдвигает границу отбора |

**Обязательно:** среди сценариев всегда есть «ничего не менять» (шаг 0 %).

### IN — от других агентов

| Источник | Что |
|---|---|
| Агент качества | прогноз серы, Т95, доверие |
| Агент надёжности | `constraints`, `regime_allowed` |
| Агент блендинга | по каждому сценарию: сводится ли смесь и почём |

### OUT — `OptimizationResult`

| Поле | Тип | Смысл |
|---|---|---|
| `scenarios` | list[Scenario] | все рассмотренные |
| `Scenario.id` | str | идентификатор |
| `Scenario.changes` | list[{tag, current, proposed}] | что менять |
| `Scenario.predicted_quality` | dict | последствия |
| `Scenario.hard_constraints_passed` | bool | прошёл жёсткие проверки |
| `Scenario.rejected_by` | list[str] | причины отбраковки |
| `Scenario.cost` | float | стоимость от блендинга |
| `Scenario.score` | float | итоговая оценка |
| `baseline_score` | float | «ничего не менять» |
| `feasible` | bool | есть ли допустимые |
| `best_scenario_id` | str / None | лучший |
| `rejection_reason` | str / None | если допустимых нет |

**Правило:** жёсткие проверки применяются **до** ранжирования. Нарушивший
выбывает, а не получает штраф.

---

## 6. АГЕНТ БЛЕНДИНГА

### IN — качество потока

| Источник | Что |
|---|---|
| Агент качества | прогноз серы, Т95 для рассматриваемого сценария |
| ЛИМС ГО точка 2 | цетановое число как фактическое значение (модели нет) |

### IN — резервуары (из фактической истории, не выдуманные)

| Резервуар | Сера | Т95 | Цетан | Откуда |
|---|---|---|---|---|
| «чистый» | 6.0 | 338 | 56.9 | p10 фактического распределения |
| «типовой» | 8.6 | 347 | 53.8 | медиана |
| «грязный» | 10.6 | 358 | 52.2 | p90 |

### IN — параметры

| Параметр | Значение | Статус |
|---|---|---|
| Сера в товарном | ≤ 10 мг/кг | **ТЗ, дословно** |
| Т95 | граница | **входной параметр**, значение — допущение |
| Цетановое число | граница | **входной параметр**, значение — допущение |
| Сумма долей | 100 % | ТЗ |
| Присадка | ≤ 3 % | организаторы |
| Цена присадки | ×100 к тонне ДТ | организаторы |
| Минимальная доля текущего потока | 50 % | **допущение**, без него задача вырождается |

### OUT — `BlendingResult`

| Поле | Тип | Смысл |
|---|---|---|
| `feasible` | bool | **вето**, если смесь не сводится |
| `fractions` | dict[tank, float] | доли, сумма 1.0 |
| `improver_pct` | float | присадка, ≤ 3 |
| `blended` | dict | сера, Т95, цетан итоговой смеси |
| `cost` | float | стоимость тонны |
| `margin_sulfur` | float | запас до предела |
| `violations` | list[str] | что нарушено, если не сводится |

---

## 7. ОРКЕСТРАТОР

### IN

| Источник | Что |
|---|---|
| Агент данных | `ProcessState` + вердикт |
| Агент качества | `QualityAssessment` |
| Агент надёжности | `ReliabilityAssessment` |
| Агент оптимизации | `OptimizationResult` |
| Агент блендинга | `BlendingResult` лучшего сценария |

### Порядок разрешения

```
1. data_quality.overall == insufficient   -> ОТКАЗ
2. reliability.regime_allowed == false    -> ОТКАЗ или только смягчение
3. optimization.feasible == false         -> ОТКАЗ
4. проблемы нет и выгода < порога         -> ВМЕШАТЕЛЬСТВО НЕ ТРЕБУЕТСЯ
5. иначе                                  -> РЕКОМЕНДАЦИЯ (самый дешёвый допустимый)
```

### OUT — `Recommendation`, 7 блоков ТЗ

| Поле | Блок ТЗ |
|---|---|
| `status` | recommendation / no_action / refusal |
| `state_summary` | 1. Время и состояние |
| `problem` | 2. Проблема / риск |
| `action` | 3. Предлагаемое действие |
| `expected_effect` | 4. Ожидаемый эффект |
| `constraints_checked` | 5. Проверка ограничений |
| `confidence` | 6. Уверенность |
| `explanation` | 7. Объяснение |
| `alternatives` | альтернативы |
| `agent_verdicts` | все вердикты — для дашборда и проверки |

---

## 8. Сводка: сколько тегов у кого

| Агент | Тегов на входе | Из них АВТ | Из них ГО |
|---|---|---|---|
| Данных | все 97 | 71 | 26 |
| Качества | 16 | 8 | 8 |
| Надёжности | 7 | 0 | 7 |
| Оптимизации | 7 рычагов | 2 | 5 |
| Блендинга | 0 | — | — |
| Оркестратора | 0 | — | — |

Блендинг и оркестратор к тегам не обращаются вообще — только к вердиктам.

---

## 9. Что ещё не решено

| Вопрос | Кого блокирует |
|---|---|
| Соответствие код → сигнал на 24-2000 | Качество, Надёжность, Оптимизация |
| Какая из `T5`/`T6`/`T11` на входе Р-202 | Надёжность (расчёт WABT) |
| Границы Т95 и цетанового числа | Блендинг |
| Включаем ли рычаги АВТ | Оптимизация |

---
Обновлено: 2026-09-12
