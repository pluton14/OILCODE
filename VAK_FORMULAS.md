# Формулы ВАК — исправленные версии

Организаторы признали ошибки в листе `ВАК` файла `Теги_хакатон.xlsx` и
прислали исправления (10.09.2026). **Используйте этот файл, а не исходный
лист.**

## Исправленные формулы

```
24-2000:GODT:T90 = 162.998 + 0.12945*T12 + 59.57*(F15/2000) + 0.00036*W7
                   + 0.26366*T23 - 424.72638*F1/F26

24-2000:GODT:T50 = 44.625 + 10.0224*P13 + 0.06981*F9 + 0.471*T6

24-2000:GODT:CloudPoint = 0.0002*F22 + 0.0021*W7 + 0.00008*F25 - 0.30656*F1
                          + 0.12018*T6 + 0.01916*F9 - 48.254 - 0.05249*T16
                          + 0.00011

24-2000:GODT:CFPP = 0.22088*T23 - 102.375 - 47.75834*P8 + 0.03862*F9
                    + 43.60207*W7 + 43.81849*P24

24-2000:GODT:T95 = 0.03814*F9 - 9.201 - 0.00002*F2 + 0.50*T6
                   + 0.48321*LIMS:24-2000.Pipeline.95%.T
```

Что изменилось относительно исходного листа:

- **T90** — появилась нормировка `F15/2000`. В исходной версии было `59.57*F15`
  при `F15 ≈ 2787`, из-за чего формула давала ~166 000 вместо температуры
  порядка 350.
- **T50** — изменился последний коэффициент (`0.471*T6`).
- **CloudPoint** — у `F22` появился коэффициент `0.0002`; в исходной версии
  переменная стояла без коэффициента, а слагаемое `+0.00011` висело без
  переменной.
- **CFPP (24-2000)** — прислана целиком.
- **T95** — прислана целиком.

## Отдельное исправление по АВТ

В формуле `AVT6:240-350:CFPP` последняя скобка лишняя. Правильно:

```
F65/F32 + F30
```

то есть **не** `F65/(F32 + F30)`.

## Что остаётся открытым

1. **`AVT6:350:T50`** выдаёт значения около 876 — это плотность, и формула
   почти дословно повторяет `AVT6:350:D15`. Исправления не прислано.

2. **Точка отбора `Pipeline`.** Формулы `24-2000:GODT:D15` и
   `24-2000:GODT:T95` используют `LIMS:24-2000.Pipeline.D15` и
   `LIMS:24-2000.Pipeline.95%.T`, но такой точки в выгрузке ЛИМС нет — есть
   только «Гидроочистка», точки 1 и 2. Ответа нет. Это единственные две
   формулы из семнадцати, которые не считаются из выданных данных.

3. **`AVT6:350:D15`** делит на `F57`, который в данных местами равен нулю —
   нужна защита от деления на ноль.

4. **Качество остальных формул.** Мы посчитали 13 формул на 36 705
   лабораторных замерах, сопоставленных с телеметрией по времени: 11 из них
   дают ошибку больше, чем простое предсказание средним значением, даже после
   устранения систематического смещения. Вопрос, актуальны ли коэффициенты,
   задан — ответа нет.

   **Практический вывод:** не считайте ВАК надёжным baseline по умолчанию.
   Сначала проверьте каждую формулу на истории, и используйте только те,
   которые реально обыгрывают среднее.

---
Обновлено: 2026-09-11
